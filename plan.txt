1. Syntaxe
	ft_strlen(str) < 1
	string vide
	open quote
	have ; \ \n in string
	start with |
	start - end with | < >
	double ||, >>>, <<<
	error ><, <>

2. Parsing
	laisser les redirections
	laisser les new_line
	separer str - space - redirection - space - str
	diviser par pipe
	diviser par direction
	change: >> file echo salut --> echo salut >> file
	change: wc -l << file --> << file wc -l

3. Executer
	Builtins --> sans pipe ou fork
	Other
		1 cmd --> fork
		> 1 cmd --> pipe + fork
